# BudE StoryBoard

An original AI-powered creative production platform.

## Setup
1. Run `./genesis.sh` to initialize the project structure[span_6](start_span)[span_6](end_span).
2. Run `npm install` to install dependencies[span_7](start_span)[span_7](end_span).
3. Use `python .bude/updates/updater.py --module <module_name>` to evolve the platform[span_8](start_span)[span_8](end_span).
